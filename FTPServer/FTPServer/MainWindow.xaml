<Window x:Class="FTPServerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FTP Server WPF" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nút Start Server -->
        <Button x:Name="btnStartServer" Content="Start Server" 
                Width="100" Height="30" 
                Click="btnStartServer_Click"
                HorizontalAlignment="Left" />

        <!-- Nút Stop Server -->
        <Button x:Name="btnStopServer" Content="Stop Server" 
                Width="100" Height="30" 
                Click="btnStopServer_Click"
                HorizontalAlignment="Left"
                Margin="120,0,0,0" />

        <!-- Ô hiển thị Log -->
        <TextBox x:Name="txtLog"
                 Grid.Row="2"
                 Margin="0,10,0,0"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 IsReadOnly="True" />
    </Grid>
</Window>